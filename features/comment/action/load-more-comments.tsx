// "use server";

// import { getComment } from '../query/get-comment';

// export async function fetchMoreCommentsAction(ticketId: string, offset: number, limit: number) {
//     const skip = offset;
//     const take = limit;
//     return await getComment({ ticketId, skip, take });
// }
